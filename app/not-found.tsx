/*
O arquivo `not-found.tsx` é um componente especial do Next.js que é renderizado quando uma rota não é encontrada. 
Ele pode ser personalizado para exibir uma mensagem ou design específico para páginas 404. 
Neste exemplo, ele simplesmente retorna um parágrafo com a mensagem "Página não encontrada".
*/

export default function NotFound() {
  return <p>Página não encontrada</p>;
}
